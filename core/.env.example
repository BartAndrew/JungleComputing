# Local development only. Generate a fresh token before running Compose:
# python3 -c "import secrets; print('JUNGLE_SERVICE_TOKEN=' + secrets.token_hex(32))" > .env
# Never expose this stack to the internet or use production customer information.
JUNGLE_SERVICE_TOKEN=
